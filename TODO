
features:
  1- session cookie lasts 12 hours
  - https
  - individual unstored chat: click to prepend '/msg nickname' in window
    - delivered to active session only
  - generic /command interface
    - /clear
    - /favicon
    - /msg
  - titlebar notification?
  - provide GMT date/time text from server by default if js isn't working
  - set cookie for initial setting of nickname, sessionlife

bugs:
  - nick collision
  - investigate send timeout after error (contributing to lost messages?)
  - scroll to bottom when new text not added
  - firefox caching issue (login -> chatlog)
  - disconnect on irc server unrecoverable
  - login page: select name/form on load
